[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["./node_modules/.bin/prisma generate", "npm run build"]

[start]
cmd = "node dist/index.js"
