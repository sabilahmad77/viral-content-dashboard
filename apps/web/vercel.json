{
  "framework": "nextjs",
  "installCommand": "npm install && cd ../../apps/api && npm install --include=dev && cd ../../apps/web",
  "buildCommand": "cd ../api && npx --yes prisma@5 generate && cd ../web && npm run build",
  "outputDirectory": ".next"
}
