{
  "framework": "nextjs",
  "installCommand": "npm install && cd ../../apps/api && npm install && cd ../../apps/web",
  "buildCommand": "npx prisma generate --schema=../api/prisma/schema.prisma && npm run build",
  "outputDirectory": ".next"
}
