{
  "framework": "nextjs",
  "installCommand": "npm install --include=dev",
  "buildCommand": "./node_modules/.bin/prisma generate --schema=../api/prisma/schema.prisma && npm run build",
  "outputDirectory": ".next"
}
