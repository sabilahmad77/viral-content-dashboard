{
  "framework": "nextjs",
  "installCommand": "npm install --include=dev",
  "buildCommand": "npx --yes prisma@5 generate --schema=../api/prisma/schema.prisma && npm run build",
  "outputDirectory": ".next"
}
