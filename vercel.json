{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate --schema=apps/api/prisma/schema.prisma && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --no-frozen-lockfile"
}
